{
  "name": "agent-task-init",
  "version": "1.0.0",
  "description": "Initialize a file-based task manager for AI agents",
  "main": "index.ts",
  "bin": {
    "agent-task-init": "index.ts"
  },
  "scripts": {
    "start": "bun run index.ts"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "private": true
}
